version: '2'
services:
    nginx:
        build: ./nginx
        network_mode: bridge
        ports:
            - "80:80"
            - "443:443"
        volumes:
            - /data/westwork/nginx/conf:/etc/nginx
            - /data/westwork/web:/usr/share/nginx/www
        restart: on-failure:5
    ldap:
        build: ./ldap
        network_mode: bridge
        expose:
            - "389"
        environment:
            - SLAPD_PASSWORD={{root_pass}}
            - SLAPD_DOMAIN={{domain}}
            - SLAPD_CONFIG_PASSWORD={{root_pass}}
        volumes:
            - /data/westwork/ldap/data:/var/lib/ldap
            - /data/westwork/ldap/conf:/etc/ldap
        restart: on-failure:5

